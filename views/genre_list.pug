extends layout

block content
  h1= title

  p #{genre_list}
  ul
    each genre in genre_list
      li
        p #{genre.url}
        a(href=genre.url) #{genre.name}
    else
      li There are no genres.